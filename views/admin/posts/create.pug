extends ../layout

block content
    body
        form(action="/admin/posts/create" method="POST")
            p Title:
                input(type="text" name="title" placeholder="")
            br
            p Content:
            textarea(id="markdown" name="body" placeholder="")
            input(type="submit")